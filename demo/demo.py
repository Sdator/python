# -*- coding: utf-8 -*-
import re

if __name__ == "__main__":
    aa = [{"var": 1, "name": 1, "cbvar": 1,
           "nameda": 1, "aename": 1}, {"nameb": 2}]

    # bb = {"a": 1, "b": 1, "c": 1, "d": 1, "e": 1}
    # bb = {k: v for k, v in aa[0].items() if k in("var", "name")}

    # new_dict = {key:val for key, val in test_dict.items() if key != 'Zhihu'}

    s = "2"
    if s in("1", "2"):
        print(s, 11)
    print(s, 222)
    # print(aa,type(aa))
